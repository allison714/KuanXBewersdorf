Create a conda environment named "ims2tiff" with the addtional packages needed:

`conda create -n ims2tiff h5py tifffile pyqt`

Then activate this env by:
`conda activate ims2tiff`

Then run the GUI:

`python ims2tiff.py`